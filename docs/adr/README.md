# ADR

架构决策记录（Architecture Decision Records）。每条记录一个**已做出的
决策**：背景、决策、后果。新决策追加编号，不回写旧记录（改变主意就
写新的 supersede）。

| 编号 | 决策 | 状态 |
|---|---|---|
| [0001](0001-interim-activation-via-env-opt-in.md) | 中间态激活走进程环境变量 opt-in，而非 Extension Manager enable | 已接受（宿主四协议落地后复审） |
